<Window x:Class="Dir.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dir"
        mc:Ignorable="d"
        Title="Dir" Height="350" Width="525"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="25"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>
            <TextBox Name="tbStartPath" Text="{Binding Path=StartPath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Name="btnStart" Click="OnLoadStarted" IsDefault="True" Grid.Column="1">Go</Button>
        </Grid>
        <TreeView Name="tvFiles" ItemsSource="{Binding Path=Files}" Grid.Row="1" />
        <StatusBar Grid.Row="2" Name="status">
            <TextBlock Text="{Binding Path=StatusText}"></TextBlock>
        </StatusBar>
    </Grid>
</Window>
